doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0, shrink-to-fit=no")
        meta(name="title", content="Modern Publisher")
        meta(name="description", content="for Modern Publisher")
        meta(name="keywords", content="gulp, webpack, publisher, sass, scss, pug, es6, babel ")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="format-detaction", content="telephone=no, address=no, email=no")
        meta(name="apple-mobile-web-app-capable", content="yes")

        title Modern Publisher
        link(rel="manifest", href="/manifest.json")
        link(rel="stylesheet", href="/css/main.css")

        script(src="https://polyfill.io/v3/polyfill.min.js?flags=gated" async defer) 
        script(src="/js/app.js" defer)

    body

        include layout/intro

        //- include layout/header

        main
            block main

        include layout/footer

        